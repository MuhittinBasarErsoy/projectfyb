<div class="skeleton-list" aria-hidden="true">
    @for (var i = 0; i < Rows; i++)
    {
        <div class="skeleton" style="width:@(100 - (i % 3) * 12)%"></div>
    }
</div>

@code {
    [Parameter] public int Rows { get; set; } = 4;
}
